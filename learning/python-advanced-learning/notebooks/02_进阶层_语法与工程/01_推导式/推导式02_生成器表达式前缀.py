# 练习时可先遮住实现自行编写。
"""生成器表达式：惰性前缀求和不超过上限。"""

def bounded_sum_positive(nums: list[int], limit: int) -> int:
    pass
if __name__ == '__main__':
    assert bounded_sum_positive([2, 3, 10, 1], limit=8) == 8
    print('ok')
